<?php
    /* function book() {
       // echo "the best book in the world"; //  if you want to use function feacure, you have to mention it.
    }
    book();

    function number() {
        $num_1 = 5;
        $num_2 = 10;
        $sum = $num_1 + $num_2;

     //   echo "the answer is $sum" . "<br>";
    }
    number();

    function simple($num){
      //  echo $num; // function with parameter (if you input value in function, you have to input in answer.)
    }
    simple(5);

    function doIt($age1, $age2){
     //   echo $age1 + $age2; // function with multi parameter.
    }
    doIt(20,24);

    function dfault($num = 5){
     //   echo $num; // default parameter (if you put default parameter in function there will be no error and answer is will show the default value but if you put another value in answer it will only show the another answer.)
    }
    dfault();

    function abc($num){
        return $num + 5; // function return
    }
    //echo abc(5);

    function dfc(){
      //  echo "Dynamic function call.";
    }
    $dycall = "dfc";
    $dycall(); 

    function fec(){
        echo "this function exit lesson"; // function exit lesson
    }
    $var = "fec";
    $doc = function_exists ($var);
    if($doc) 
        $var();
    else
        echo "this function is does not exit.";
    
    $hello =20;
    function hero(){
        global $hello;
        echo $hello; // global and local variable
    }
    hero(); */

    
   
?>